using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Frozen : SpellCard
{

    // Start is called before the first frame update
    void Start()
    {
        base.SetCard();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
