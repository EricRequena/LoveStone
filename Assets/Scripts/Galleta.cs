using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Galleta : CharacterCard
{
    // Start is called before the first frame update
    void Start()
    {
        base.SetCard();

        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
