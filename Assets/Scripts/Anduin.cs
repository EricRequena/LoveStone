using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Anduin : Avatar
{
    // Start is called before the first frame update
    void Start()
    {
        base.SetAvatar();
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
